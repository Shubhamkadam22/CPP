#include <iostream> 
using namespace std; 
//variables in cpp is a storage location with a name which holds a value that can be changed during program excution. 
//data tpes in cpp are used to specify the type of data that a variable can hold. 

int main() {
int a , b; 
cout << "Enter two numbers: ";
cin >> a >> b;
cout << "The numbers are: " << a << " and " << b << endl;
return 0;
}
